# Notebooks

这里放探索式研究 notebook。

建议命名方式：

- `YYYYMMDD_topic.ipynb`
- `factor_<name>_research.ipynb`
- `strategy_<name>_prototype.ipynb`

不要把大体量中间结果直接写在 notebook 同目录，统一写到 `research/output/` 或 `data/` 下的受控路径。
